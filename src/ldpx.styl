.ldpx
  perspective: 8px
  perspective-origin: 50% 50%
  overflow-y: scroll
  overflow-x: hidden
.ldpx-full
  width: 100%
  height: 100%

for idx in 1 2 3 4 5 6 7 8
  .ldpxl.z-{idx}
    transform: translateZ(idx * 1px)
  .ldpxl.zs-{idx}
    transform: translateZ(idx * 1px) scale( (8 - idx ) /  8)
